'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';
import { CheckCircle2, FileText, FolderInput, Loader2, Sparkles, UserPlus, X } from 'lucide-react';

type Intake = {
  id: string; sourceFolderName: string; extractedName?: string | null; extractedCpf?: string | null;
  suggestedArea?: string | null; suggestedClient?: { id: string; name: string; cpfCnpj: string } | null;
  files: Array<{ id: string; title: string; classification?: string | null }>;
};

export default function EntradaPage() {
  const [folders, setFolders] = useState<Intake[]>([]);
  const [loading, setLoading] = useState(true);
  const [preview, setPreview] = useState<{ id: string; title: string } | null>(null);

  useEffect(() => {
    fetch('/api/intake', { cache: 'no-store' }).then((r) => r.json()).then((data) => setFolders(data.folders || [])).finally(() => setLoading(false));
  }, []);

  return <main className="mx-auto max-w-6xl space-y-6 pb-12">
    <header className="flex flex-col gap-3 border-b border-slate-200 pb-6 md:flex-row md:items-end md:justify-between">
      <div><p className="text-[10px] font-extrabold uppercase tracking-[.2em] text-[#B68B1C]">Drive conectado</p><h1 className="mt-2 font-heading text-3xl font-extrabold tracking-tight text-[#071B3A]">Central de Entrada</h1><p className="mt-1.5 max-w-2xl text-sm text-slate-500">Revise documentos que chegaram pelo Drive antes de vinculá-los a um cliente ou processo.</p></div>
      <span className="inline-flex items-center gap-2 rounded-xl border border-[#e4d09a] bg-[#fffaf0] px-4 py-3 text-xs font-bold text-[#705613]"><Sparkles className="h-4 w-4" /> Triagem assistida</span>
    </header>
    <section className="rounded-[26px] border border-blue-100 bg-gradient-to-r from-[#071B3A] to-[#143365] p-6 text-white shadow-lg"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><p className="text-[10px] font-extrabold uppercase tracking-[.18em] text-[#D4AF37]">Como funciona</p><p className="mt-2 text-sm leading-6 text-slate-200">Crie uma pasta com o nome da cliente e organize nela os documentos e subpastas. Cada pasta principal gera apenas um atendimento para conferência.</p></div><span className="shrink-0 rounded-xl bg-white/10 px-4 py-3 text-xs font-bold">Nada é cadastrado sem validação</span></div></section>
    {loading ? <div className="flex min-h-64 items-center justify-center"><Loader2 className="h-7 w-7 animate-spin text-blue-600" /></div> : !folders.length ? <section className="rounded-[26px] border border-dashed border-slate-300 bg-white px-6 py-16 text-center"><FolderInput className="mx-auto h-10 w-10 text-slate-300" /><h2 className="mt-4 font-heading text-lg font-extrabold text-[#071B3A]">Nenhuma pasta recebida ainda</h2><p className="mx-auto mt-2 max-w-md text-xs leading-5 text-slate-500">Quando você adicionar arquivos à pasta configurada do Drive, eles aparecerão aqui para revisão.</p></section> : <section className="grid gap-4 md:grid-cols-2">{folders.map((folder) => <article key={folder.id} className="rounded-[24px] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-[10px] font-extrabold uppercase tracking-[.16em] text-[#B68B1C]">Aguardando revisão</p><h2 className="mt-1 truncate font-heading text-base font-extrabold text-[#071B3A]">{folder.suggestedClient?.name || folder.extractedName || folder.sourceFolderName}</h2><p className="mt-1 truncate text-[11px] text-slate-400">Pasta: {folder.sourceFolderName}</p></div><span className="rounded-full bg-blue-50 px-2.5 py-1 text-[10px] font-extrabold text-blue-700">{folder.files.length} arquivo{folder.files.length === 1 ? '' : 's'}</span></div><div className="mt-4 rounded-xl bg-slate-50 p-3 text-xs"><p className="font-extrabold text-[#071B3A]">{folder.suggestedClient ? `Cliente identificado: ${folder.suggestedClient.name}` : folder.extractedCpf ? `CPF identificado: ${folder.extractedCpf}` : 'Dados em leitura automática'}</p><p className="mt-1 text-slate-500">{folder.suggestedClient ? 'Cadastro existente encontrado para conferência.' : 'Confira a sugestão antes de criar ou vincular o cadastro.'}{folder.suggestedArea ? ` Área sugerida: ${folder.suggestedArea}.` : ''}</p></div><div className="mt-4 max-h-64 space-y-1.5 overflow-y-auto pr-1">{folder.files.map((file) => <button key={file.id} type="button" onClick={() => setPreview({ id: file.id, title: file.title })} className="flex w-full items-center gap-2 rounded-lg p-2 text-left text-xs hover:bg-blue-50"><FileText className="h-3.5 w-3.5 shrink-0 text-slate-400" /><span className="min-w-0 flex-1 truncate font-medium text-slate-600">{file.title}</span><span className="shrink-0 text-[10px] text-slate-400">{file.classification || 'Documento'}</span><span className="shrink-0 text-[10px] font-bold text-blue-700">Ver</span></button>)}</div><div className="mt-5 flex gap-2 border-t border-slate-100 pt-4">{folder.suggestedClient ? <Link href={`/processos?clienteId=${folder.suggestedClient.id}`} className="inline-flex flex-1 items-center justify-center gap-1.5 rounded-xl bg-[#071B3A] px-3 py-2.5 text-xs font-bold text-white"><CheckCircle2 className="h-3.5 w-3.5" /> Abrir cliente</Link> : <Link href={`/clientes?novo=true&nome=${encodeURIComponent(folder.extractedName || folder.sourceFolderName)}&area=${encodeURIComponent(folder.suggestedArea || 'Previdenciário')}`} className="inline-flex flex-1 items-center justify-center gap-1.5 rounded-xl bg-[#071B3A] px-3 py-2.5 text-xs font-bold text-white"><UserPlus className="h-3.5 w-3.5" /> Conferir pré-cadastro</Link>}</div></article>)}</section>}
    {preview && <div className="fixed inset-0 z-[90] flex items-center justify-center bg-slate-950/60 p-4"><section className="flex h-[88vh] w-full max-w-6xl flex-col overflow-hidden rounded-2xl bg-white shadow-2xl"><header className="flex items-center justify-between bg-[#071B3A] px-5 py-4 text-white"><div className="min-w-0"><p className="text-[10px] font-extrabold uppercase tracking-[.16em] text-[#D4AF37]">Documento recebido</p><h2 className="truncate text-sm font-bold">{preview.title}</h2></div><button type="button" onClick={() => setPreview(null)} aria-label="Fechar visualização" className="rounded-lg p-2 hover:bg-white/10"><X className="h-5 w-5" /></button></header><iframe title={preview.title} src={`/api/intake/file/${preview.id}`} className="min-h-0 flex-1 bg-slate-100" /><footer className="flex justify-end border-t border-slate-200 p-3"><button type="button" onClick={() => setPreview(null)} className="rounded-lg bg-[#071B3A] px-4 py-2 text-xs font-bold text-white">Fechar</button></footer></section></div>}
  </main>;
}
